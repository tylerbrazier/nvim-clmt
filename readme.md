Neovim Command Line Mode Tools
==============================

- shows the state of `ignorecase` when searching (`ctrl-x` to toggle)
- in filename completion press `/` after a dir to show its contents
  (then `<Tab>` to continue completion inside)
